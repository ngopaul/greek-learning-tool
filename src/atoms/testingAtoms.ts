import { atom } from 'jotai';

// Atom to store the current lesson
// export const currentLessonAtom = atom<string | null>(null);

// Atom to store the list of available lessons
export const startTestingAtom = atom<boolean>(false);